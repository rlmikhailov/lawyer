// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://rlmikhailov.github.io',
  base: '/lawyer/', // Use '/' if you deploy to <username>.github.io root repo
});
