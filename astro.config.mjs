// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://rlmikhailov.github.io',
  base: '/', // Use '/' if you deploy to <username>.github.io root repo
});
